Recent test shows The AI is still attempting to respond for user.
Inner self also shows at first instead of remaining hidden.
It should be kept as some kind of tiny little clickable expandable button.
About one fourth the size of the play area button at the beginning of the
AI's response.

We are going to implement a strict Context Filter Matrix within our
core/inference.py and tools/router.py pipelines. The goal is to ensure that
while both the Companion and Storyteller modes can access shared general
knowledge, they are strictly locked out of each other's specific data pools.

Implement the memory filtering rules exactly like this:

1. Shared Memory Space (Accessible by BOTH Pipelines)
Data Sources: General wiki data arrays, Wikipedia dumps, and global static
knowledge.

Rule: Both modes can run vector queries here to pull objective facts or
reference data.

2. Companion-Only Memory Space (STRICTLY LOCKED to Storyteller)
Data Sources: The Hyro_HiRAG emotional memory index,
storage/emotional_memory.py logs, and my personal research files.

Rule: This is exclusively used to build the human-like relationship matrix
 and inner-self reflection loop for Meta-Hyro. The Storyteller mode must
have zero access to this data.

3. Storyteller-Only Memory Space (STRICTLY LOCKED to Companion)
Data Sources: sandbox_workspace/state/world_state.json, active StoryCards,
Narrator prompt template, and campaign-specific scenario data.

Rule: This is used strictly to maintain RPG mechanics, inventory, party
arrays (like the Mascot slot rules), and narrative continuity. The Companion
mode must have zero access to this data.

Objective:
Write the context assembly functions in core/inference.py. When a user submits
a prompt, look at the active mode flag. Intercept the prompt, query ONLY the
permitted memory spaces for that specific mode, combine the retrieved context
with the user's input, and then pass that final filtered payload to our local
inference engine. Do not let the contexts contaminate each other.

Ask follow-up questions as needed.